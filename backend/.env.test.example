# Base de test — option Docker locale (voir backend/.env.docker.example)
# Ou une seconde base cloud dédiée aux tests.
DATABASE_URL="postgresql://admin:password@localhost:5433/restaurant_test?schema=public"
JWT_SECRET="test-secret-local-only"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
